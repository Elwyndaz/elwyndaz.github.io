worker/.wrangler/

# graft's local graph cache, regenerable, not committed (run `graft build`).
graft/

# Jekylls lokala bygge. Regenereras med `jekyll build`, GitHub Pages bygger sitt eget.
_site/
.jekyll-cache/

# playwright-cli skriver sidsnapshots i den mapp kommandot kors ifran.
.playwright-cli/

# Internt designunderlag. Ligger i reporoten men ska ALDRIG publiceras:
# Jekyll kopierar annars mappen rakt ut till orgutveckling.se.
Webbanalys och strategi/
Webbanalys och strategi.zip
HANDOFF-REDESIGN.md

# repowise local index - regenerable, not committed (run `repowise update`).
.repowise/

# Maskinspecifik verktygskonfiguration. Bada innehaller absoluta sokvagar till
# C:\dev och gar inte att dela med en annan dator.
.mcp.json
.vscode/

# AGENTS.md har bara repowise autogenererade distill-avsnitt i det har repot,
# inga egna instruktioner. Halls lokalt. Skriver du riktiga instruktioner har:
# ta bort raden nedan forst, annars hamnar de aldrig i git.
AGENTS.md
